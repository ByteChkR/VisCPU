#import "link D:\Users\Tim\Documents\viscpu\HL\Kernel\Program.vbin"

public static void APP_START(uint args, uint argc)
{
	uint echoLen;
	uint echoStart = arg_get(1, args, argc, &echoLen);
	uint binLen = echoStart -  args;
	echoLen = argc - binLen;
	kprintl(echoStart, echoLen);
}