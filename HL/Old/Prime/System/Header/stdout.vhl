#define prints(str)
{
	Console.Write(&str, size_of(str));
}

#define printl(str)
{
	Console.WriteLine(&str, size_of(str));
}

#define printl(str)
{
	Console.WriteLine(&str, size_of(str));
}

#define clear()
{
	Console.Clear();
}

#define printn(n)
{
	Console.WriteNumber(n);
}

#define printnl(n)
{
	Console.WriteNumberLine(n);
}

