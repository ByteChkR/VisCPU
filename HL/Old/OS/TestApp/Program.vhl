//Entry Point of Project

#import "link ..\OS\Program.vbin"

string sysLib = "libs/system.vbin";
string cWChar = "SFUN_Console_WriteChar";

uint sysLibPtr;
uint cWriteChar;

public void APP_START(string args, uint argc)
{
	OS_WriteChar('A');
	sysLibPtr = OS_LoadLib(&sysLib, size_of(sysLib));
	cWriteChar = OS_ResolveCall(sysLibPtr, &cWChar, size_of(cWChar));
	cWriteChar('B');
}