
#include "Console.vhl"
#include "Exception.vhl"

public class ExceptionHandler
{
	public static void Throw(Exception ex)
	{
		Console.WriteLine(ex.GetMessage(), ex.GetMessageLength());
		interrupt(1); //Print Stacktrace and continue here
		halt(); //Halt Execution
	}
}
